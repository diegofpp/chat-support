# Database
# URL de conexión a la base de datos PostgreSQL (Neon)
DATABASE_URL="postgresql://user:password@host:port/database"

# NextAuth
# URL base de la aplicación (http://localhost:3000 en desarrollo)
NEXTAUTH_URL="http://localhost:3000"
# Secreto para firmar las cookies de sesión (generar con: openssl rand -base64 32)
NEXTAUTH_SECRET="your-secret-key-here"

# OpenAI (para el chat de soporte)
# API Key de OpenAI para el chat
OPENAI_API_KEY="your-openai-api-key-here"

# Notas:
# 1. Copiar este archivo a .env y reemplazar los valores con los reales
# 2. Nunca subir el archivo .env al repositorio
# 3. Mantener actualizado este archivo cuando se agreguen nuevas variablesz